Sym_55 <- function(u, n) 
{
  .arg_u <- u
  .arg_n <- n
  idx <- (1.0 + floor((.arg_u * .arg_n)))
  if ((idx < 1.0)) {
    idx <- 1.0
  } else {
  }
  if ((idx > .arg_n)) {
    idx <- n
  } else {
  }
  return(idx)
}
