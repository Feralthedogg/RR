Sym_11 <- function(xs) 
{
  .arg_xs <- xs
  n <- length(.arg_xs)
  out <- rep.int(0.0, n)
  i <- 1.0
  repeat {
    if (!(i <= length(.arg_xs))) break
    rr_mark(69L, 9L);
    out[i] <- .arg_xs[i]
    i <- (i + 1.0)
    next
  }
  return(out)
}
