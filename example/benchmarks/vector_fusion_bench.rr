import "../common/sim_utils.rr"

fn main() {
    n = 2048.0
    x = seq_len(n)
    y = seq_len(n)
    z = rep.int(0.0, n)
    step = 1.0
    while (step <= 24.0) {
        i = 1.0
        while (i <= n) {
            z[i] = (x[i] + y[i]) * 0.5 + x[i] * 0.25 - y[i] * 0.10
            i += 1.0
        }
        x = copy_vec(z)
        step += 1.0
    }
    print_metric("vector_fusion_tail", z[n])
    print_metric("vector_fusion_mean", vector_mean(z))
}

main()
