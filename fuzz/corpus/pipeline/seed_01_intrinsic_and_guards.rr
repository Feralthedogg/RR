fn call_abs(n: int) {
  let x = seq_len(n) - 4
  let y = seq_len(n)
  for (i in 1..length(x)) {
    y[i] = abs(x[i])
  }
  return y
}

fn cond(flag: bool, a: int, b: int) -> int {
  if (flag) {
    return a
  } else {
    return b
  }
}

print(call_abs(5L))
print(cond(TRUE, 1L, 2L))
